// dot notations.................
const myself={
    name:'mim',
    roll:522929,
    reg:3404590,
    address:'rajshahi',
}

console.log(myself);
// dot notations
const user = myself.name;
console.log(user);

// bracket notations
const userAddress = myself['address'];
console.log(userAddress);
